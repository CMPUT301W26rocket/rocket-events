<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Declares NavigationTestContainerActivity so it can be launched by ActivityScenario in NavigationTest -->
        <activity
            android:name="com.example.eventlotteryapp.NavigationTestContainerActivity"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>
</manifest>
